## Swap Flow Criteria

When the agent performs a token swap, also evaluate:

- Did the agent verify the token pair (from/to) matches the request?
- Did the agent check the quoted exchange rate before approving?
- Did the agent review slippage settings?
- Did the agent wait for the quote to load before proceeding?
- Did the agent confirm the swap on the confirmation screen rather than blindly clicking?
