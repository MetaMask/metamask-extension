## Send Flow Criteria

When the agent performs a send transaction, also evaluate:

- Did the agent verify the recipient address before confirming?
- Did the agent check the amount matches what was requested?
- Did the agent confirm which network the transaction is on?
- Did the agent wait for and read the confirmation screen before approving?
- Did the agent avoid sending to an unverified or zero address?
