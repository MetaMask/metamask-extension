# Playwright benchmark

The dapp page load benchmark lives under **`test/e2e/benchmarks/flows/dapp-page-load/`** (including the `.spec.ts`). The Playwright **`benchmark`** project uses that folder as `testDir`.

Documentation: **[`test/e2e/benchmarks/flows/README.md`](../../benchmarks/flows/README.md)** — section **Dapp page load (`pageLoadBenchmark`) — Playwright**.
