import withModalProps from '../../../../../helpers/higher-order-components/with-modal-props';
import TransactionFailed from './transaction-failed.component';

export default withModalProps(TransactionFailed);
