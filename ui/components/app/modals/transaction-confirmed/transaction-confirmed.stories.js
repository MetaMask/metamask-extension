import React from 'react';
import TransactionConfirmed from '.';

export default {
  title: 'Components/UI/TransactionConfirmed', // title should follow the folder structure location of the component. Don't use spaces.
  id: __filename,
};

export const DefaultStory = () => <TransactionConfirmed />;

DefaultStory.storyName = 'Default';
