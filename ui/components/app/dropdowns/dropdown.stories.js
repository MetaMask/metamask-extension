import React from 'react';
import { Dropdown } from './dropdown';

export default {
  title: 'Components/UI/Dropdown', // title should follow the folder structure location of the component. Don't use spaces.
  id: __filename,
};

export const DefaultStory = () => <Dropdown />;

DefaultStory.storyName = 'Default';
