import { Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Template } from './template';

### Use this Template folder when starting a component from scratch.

# Template

Template is a graphical element that allows users to select one or more options from a set of choices.

<Canvas>
  <Story id="components-componentlibrary-template--default-story" />
</Canvas>

## Props

<ArgsTable of={Template} />

### Demo

Use the `Template` when starting a new DS component from scratch

<Canvas>
  <Story id="components-componentlibrary-template--demo" />
</Canvas>

```jsx
import { Template } from '../../component-library';

<Template />;
```
