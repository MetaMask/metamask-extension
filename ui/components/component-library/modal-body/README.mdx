import { Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { ModalBody } from './modal-body';

# ModalBody

`ModalBody` is a container component that handles the scrolling of the `Modal` content

<Canvas>
  <Story id="components-componentlibrary-modalbody--default-story" />
</Canvas>

## Props

The `ModalBody` accepts all props below as well as all [Box](/docs/components-ui-box--default-story#props) component props

<ArgsTable of={ModalBody} />
