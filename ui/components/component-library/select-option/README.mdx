import { Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { SelectOption } from './select-option';

### Use this SelectOption folder when starting a component from scratch.

# SelectOption

SelectOption is a graphical element that allows users to select one or more options from a set of choices.

<Canvas>
  <Story id="components-componentlibrary-select-option--default-story" />
</Canvas>

## Props

<ArgsTable of={SelectOption} />

### Demo

Use the `SelectOption` when starting a new DS component from scratch

<Canvas>
  <Story id="components-componentlibrary-select-option--demo" />
</Canvas>

```jsx
import { SelectOption } from '../../component-library';

<SelectOption />;
```
