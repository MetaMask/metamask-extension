import { Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { SelectButton } from './select-button';

### Use this SelectButton folder when starting a component from scratch.

# SelectButton

SelectButton is a graphical element that allows users to select one or more options from a set of choices.

<Canvas>
  <Story id="components-componentlibrary-selectbutton--default-story" />
</Canvas>

## Props

<ArgsTable of={SelectButton} />

### Demo

Use the `SelectButton` when starting a new DS component from scratch

<Canvas>
  <Story id="components-componentlibrary-selectbutton--demo" />
</Canvas>

```jsx
import { SelectButton } from '../../component-library';

<SelectButton />;
```
