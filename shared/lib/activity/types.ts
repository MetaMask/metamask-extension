export type {
  ActivityItem,
  ActivityKind,
  Status,
  TokenAmount,
  FiatAmount,
  Fee as ActivityFee,
} from '@metamask/client-utils';

export type ActivityListItem = import('@metamask/client-utils').ActivityItem;
