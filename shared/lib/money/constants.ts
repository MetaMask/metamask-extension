/**
 * mUSD is pegged 1:1 to USD, so money-account fiat amounts always display as
 * USD (same currency code used by other MM Pay surfaces such as perps).
 */
export const MONEY_ACCOUNT_FIAT_CURRENCY = 'usd';
