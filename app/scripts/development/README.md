# Development-only files

This folder is for files that are only used during development.

Code in this folder may support local debugging, profiling, or developer tooling, but it should not contain production runtime behavior or shared application constants.
