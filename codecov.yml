codecov:
  bot: 'codecov-io'
  require_ci_to_pass: yes
coverage:
  round: nearest
  status:
    project:
      global:
        target: auto
        threshold: 0%
        base: auto
      transforms:
        target: 100%
        threshold: 0%
        paths:
          - development/build/transforms/**/*.js
