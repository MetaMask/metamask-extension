machine:
  node:
    version: 8.1.4
dependencies:
  pre:
    - "npm i -g testem"
    - "npm i -g mocha"
test:
  override:
    - "npm run lint"
    - "npm run test-coverage"
    - "npm run test-integration"