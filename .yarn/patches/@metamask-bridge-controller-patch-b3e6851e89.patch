diff --git a/dist/bridge-controller-method-action-types.d.cts b/dist/bridge-controller-method-action-types.d.cts
index c3366f67dfcd203737deabcb854400b60db6d1a2..ab1c49f0a19f0c1096e3e12773d7d155fd0551ff 100644
--- a/dist/bridge-controller-method-action-types.d.cts
+++ b/dist/bridge-controller-method-action-types.d.cts
@@ -19,6 +19,10 @@ export type BridgeControllerSetLocationAction = {
     type: `BridgeController:setLocation`;
     handler: BridgeController['setLocation'];
 };
+export type BridgeControllerGetLocationAction = {
+    type: `BridgeController:getLocation`;
+    handler: BridgeController['getLocation'];
+};
 export type BridgeControllerResetStateAction = {
     type: `BridgeController:resetState`;
     handler: BridgeController['resetState'];
@@ -38,5 +42,5 @@ export type BridgeControllerUpdateBatchSellTradesAction = {
 /**
  * Union of all BridgeController action types.
  */
-export type BridgeControllerMethodActions = BridgeControllerUpdateBridgeQuoteRequestParamsAction | BridgeControllerFetchQuotesAction | BridgeControllerStopPollingForQuotesAction | BridgeControllerSetLocationAction | BridgeControllerResetStateAction | BridgeControllerSetChainIntervalLengthAction | BridgeControllerTrackUnifiedSwapBridgeEventAction | BridgeControllerUpdateBatchSellTradesAction;
+export type BridgeControllerMethodActions = BridgeControllerUpdateBridgeQuoteRequestParamsAction | BridgeControllerFetchQuotesAction | BridgeControllerUpdateBatchSellTradesAction | BridgeControllerStopPollingForQuotesAction | BridgeControllerSetLocationAction | BridgeControllerGetLocationAction | BridgeControllerResetStateAction | BridgeControllerSetChainIntervalLengthAction | BridgeControllerTrackUnifiedSwapBridgeEventAction;
 //# sourceMappingURL=bridge-controller-method-action-types.d.cts.map
\ No newline at end of file
diff --git a/dist/bridge-controller-method-action-types.d.mts b/dist/bridge-controller-method-action-types.d.mts
index eb4b39d539ab7cc00b8d703d4794dae15a80a3ce..e21e5bb7710c70c86a287a2112167feaea284a32 100644
--- a/dist/bridge-controller-method-action-types.d.mts
+++ b/dist/bridge-controller-method-action-types.d.mts
@@ -19,6 +19,10 @@ export type BridgeControllerSetLocationAction = {
     type: `BridgeController:setLocation`;
     handler: BridgeController['setLocation'];
 };
+export type BridgeControllerGetLocationAction = {
+    type: `BridgeController:getLocation`;
+    handler: BridgeController['getLocation'];
+};
 export type BridgeControllerResetStateAction = {
     type: `BridgeController:resetState`;
     handler: BridgeController['resetState'];
@@ -38,5 +42,5 @@ export type BridgeControllerUpdateBatchSellTradesAction = {
 /**
  * Union of all BridgeController action types.
  */
-export type BridgeControllerMethodActions = BridgeControllerUpdateBridgeQuoteRequestParamsAction | BridgeControllerFetchQuotesAction | BridgeControllerStopPollingForQuotesAction | BridgeControllerSetLocationAction | BridgeControllerResetStateAction | BridgeControllerSetChainIntervalLengthAction | BridgeControllerTrackUnifiedSwapBridgeEventAction | BridgeControllerUpdateBatchSellTradesAction;
+export type BridgeControllerMethodActions = BridgeControllerUpdateBridgeQuoteRequestParamsAction | BridgeControllerFetchQuotesAction | BridgeControllerUpdateBatchSellTradesAction | BridgeControllerStopPollingForQuotesAction | BridgeControllerSetLocationAction | BridgeControllerGetLocationAction | BridgeControllerResetStateAction | BridgeControllerSetChainIntervalLengthAction | BridgeControllerTrackUnifiedSwapBridgeEventAction;
 //# sourceMappingURL=bridge-controller-method-action-types.d.mts.map
\ No newline at end of file
diff --git a/dist/bridge-controller.cjs b/dist/bridge-controller.cjs
index bc79879d97cb812307e70d1a6731bf1720ca2e26..0b763209e850d38b999bcae78ce415686ea8fdfc 100644
--- a/dist/bridge-controller.cjs
+++ b/dist/bridge-controller.cjs
@@ -128,6 +128,7 @@ const MESSENGER_EXPOSED_METHODS = [
     'updateBatchSellTrades',
     'stopPollingForQuotes',
     'setLocation',
+    'getLocation',
     'resetState',
     'setChainIntervalLength',
     'trackUnifiedSwapBridgeEvent',
@@ -152,7 +153,7 @@ class BridgeController extends (0, polling_controller_1.StaticIntervalPollingCon
          * Set via setLocation() before navigating to the swap/bridge flow.
          * Used as default for all subsequent internal events.
          */
-        _BridgeController_location.set(this, constants_1.MetaMetricsSwapsEventSource.MainView);
+        _BridgeController_location.set(this, constants_1.MetaMetricsSwapsEventSource.Unknown);
         _BridgeController_clientId.set(this, void 0);
         _BridgeController_clientVersion.set(this, void 0);
         _BridgeController_getLayer1GasFee.set(this, void 0);
@@ -440,6 +441,14 @@ class BridgeController extends (0, polling_controller_1.StaticIntervalPollingCon
         this.setLocation = (location) => {
             __classPrivateFieldSet(this, _BridgeController_location, location, "f");
         };
+        /**
+         * Returns the location/entry point for the current swap or bridge flow.
+         *
+         * @returns The entry point from which the user initiated the flow
+         */
+        this.getLocation = () => {
+            return __classPrivateFieldGet(this, _BridgeController_location, "f");
+        };
         this.resetState = (reason = constants_1.AbortReason.ResetState, quoteRequestIndex = null) => {
             this.stopPollingForQuotes(reason);
             this.update((state) => {
diff --git a/dist/bridge-controller.d.cts b/dist/bridge-controller.d.cts
index 2b143274b03c95a30465f74267ad027bc1abdf3e..0391f86cecae2e6cac793bed45a9b971ef7eafcd 100644
--- a/dist/bridge-controller.d.cts
+++ b/dist/bridge-controller.d.cts
@@ -102,6 +102,12 @@ export declare class BridgeController extends BridgeController_base<typeof BRIDG
      * @param location - The entry point from which the user initiated the flow
      */
     setLocation: (location: MetaMetricsSwapsEventSource) => void;
+    /**
+     * Returns the location/entry point for the current swap or bridge flow.
+     *
+     * @returns The entry point from which the user initiated the flow
+     */
+    getLocation: () => MetaMetricsSwapsEventSource;
     resetState: (reason?: AbortReason, quoteRequestIndex?: number | null) => void;
     /**
      * Sets the interval length based on the source chain
diff --git a/dist/bridge-controller.d.mts b/dist/bridge-controller.d.mts
index fde92107cd95beef9d9b77c9f28e3fcfa6d9a9ef..c6bb6ab4dc4bc82c0d0f55fa6670170f640baadf 100644
--- a/dist/bridge-controller.d.mts
+++ b/dist/bridge-controller.d.mts
@@ -102,6 +102,12 @@ export declare class BridgeController extends BridgeController_base<typeof BRIDG
      * @param location - The entry point from which the user initiated the flow
      */
     setLocation: (location: MetaMetricsSwapsEventSource) => void;
+    /**
+     * Returns the location/entry point for the current swap or bridge flow.
+     *
+     * @returns The entry point from which the user initiated the flow
+     */
+    getLocation: () => MetaMetricsSwapsEventSource;
     resetState: (reason?: AbortReason, quoteRequestIndex?: number | null) => void;
     /**
      * Sets the interval length based on the source chain
diff --git a/dist/bridge-controller.mjs b/dist/bridge-controller.mjs
index e85fbf3f8f3e7f2951b04787b7c9ec63ac68aa90..d76701c08109f2e1cadef5b0b1b34e57a712f5ff 100644
--- a/dist/bridge-controller.mjs
+++ b/dist/bridge-controller.mjs
@@ -125,6 +125,7 @@ const MESSENGER_EXPOSED_METHODS = [
     'updateBatchSellTrades',
     'stopPollingForQuotes',
     'setLocation',
+    'getLocation',
     'resetState',
     'setChainIntervalLength',
     'trackUnifiedSwapBridgeEvent',
@@ -149,7 +150,7 @@ export class BridgeController extends StaticIntervalPollingController() {
          * Set via setLocation() before navigating to the swap/bridge flow.
          * Used as default for all subsequent internal events.
          */
-        _BridgeController_location.set(this, MetaMetricsSwapsEventSource.MainView);
+        _BridgeController_location.set(this, MetaMetricsSwapsEventSource.Unknown);
         _BridgeController_clientId.set(this, void 0);
         _BridgeController_clientVersion.set(this, void 0);
         _BridgeController_getLayer1GasFee.set(this, void 0);
@@ -437,6 +438,14 @@ export class BridgeController extends StaticIntervalPollingController() {
         this.setLocation = (location) => {
             __classPrivateFieldSet(this, _BridgeController_location, location, "f");
         };
+        /**
+         * Returns the location/entry point for the current swap or bridge flow.
+         *
+         * @returns The entry point from which the user initiated the flow
+         */
+        this.getLocation = () => {
+            return __classPrivateFieldGet(this, _BridgeController_location, "f");
+        };
         this.resetState = (reason = AbortReason.ResetState, quoteRequestIndex = null) => {
             this.stopPollingForQuotes(reason);
             this.update((state) => {
diff --git a/dist/index.d.cts b/dist/index.d.cts
index 57c3c89e85e96e1e7b30b1bc55c837debb0f80c0..f6a9bde6453bd6ed90c3b5242fb994ea7c5f3e04 100644
--- a/dist/index.d.cts
+++ b/dist/index.d.cts
@@ -3,7 +3,7 @@ export { UnifiedSwapBridgeEventName, UNIFIED_SWAP_BRIDGE_EVENT_CATEGORY, InputAm
 export type { AccountHardwareType, RequiredEventContextFromClient, CrossChainSwapsEventProperties, TradeData, RequestParams, RequestMetadata, TxStatusData, QuoteFetchData, QuoteWarning, } from "./utils/metrics/types.cjs";
 export { getAccountHardwareType, formatProviderLabel, getRequestParams, getSwapType, isHardwareWallet, isCustomSlippage, getQuotesReceivedProperties, } from "./utils/metrics/properties.cjs";
 export type { ChainConfiguration, L1GasFees, NonEvmFees, QuoteMetadata, GasMultiplierByChainId, FeatureFlagResponse, BridgeAsset, GenericQuoteRequest, Protocol, BatchSellTradesResponse, GaslessProperties, SimulatedGasFeeLimits, TokenAmountValues, Step, RefuelData, Quote, QuoteResponse, FeeData, TxData, Intent, IntentOrderLike, BitcoinTradeData, TronTradeData, BridgeControllerState, BridgeControllerAction, BridgeControllerActions, BridgeControllerEvents, BridgeControllerMessenger, FeatureFlagsPlatformConfig, TxFeeGasLimits, } from "./types.cjs";
-export type { BridgeControllerUpdateBridgeQuoteRequestParamsAction, BridgeControllerFetchQuotesAction, BridgeControllerStopPollingForQuotesAction, BridgeControllerSetLocationAction, BridgeControllerResetStateAction, BridgeControllerSetChainIntervalLengthAction, BridgeControllerTrackUnifiedSwapBridgeEventAction, BridgeControllerUpdateBatchSellTradesAction, } from "./bridge-controller-method-action-types.cjs";
+export type { BridgeControllerUpdateBridgeQuoteRequestParamsAction, BridgeControllerFetchQuotesAction, BridgeControllerStopPollingForQuotesAction, BridgeControllerSetLocationAction, BridgeControllerGetLocationAction, BridgeControllerResetStateAction, BridgeControllerSetChainIntervalLengthAction, BridgeControllerTrackUnifiedSwapBridgeEventAction, BridgeControllerUpdateBatchSellTradesAction, } from "./bridge-controller-method-action-types.cjs";
 export { AbortReason } from "./utils/metrics/constants.cjs";
 export { StatusTypes } from "./types.cjs";
 export { AssetType, SortOrder, ChainId, RequestStatus, type TokenFeature, type QuoteStreamCompleteData, type BridgeControllerGetStateAction, type BridgeControllerStateChangeEvent, } from "./types.cjs";
diff --git a/dist/index.d.mts b/dist/index.d.mts
index 123315f0b9cd24d97c0a62325bbf63500e1dc704..d595899a4f056e2a5a6c2ed235557f350980f408 100644
--- a/dist/index.d.mts
+++ b/dist/index.d.mts
@@ -3,7 +3,7 @@ export { UnifiedSwapBridgeEventName, UNIFIED_SWAP_BRIDGE_EVENT_CATEGORY, InputAm
 export type { AccountHardwareType, RequiredEventContextFromClient, CrossChainSwapsEventProperties, TradeData, RequestParams, RequestMetadata, TxStatusData, QuoteFetchData, QuoteWarning, } from "./utils/metrics/types.mjs";
 export { getAccountHardwareType, formatProviderLabel, getRequestParams, getSwapType, isHardwareWallet, isCustomSlippage, getQuotesReceivedProperties, } from "./utils/metrics/properties.mjs";
 export type { ChainConfiguration, L1GasFees, NonEvmFees, QuoteMetadata, GasMultiplierByChainId, FeatureFlagResponse, BridgeAsset, GenericQuoteRequest, Protocol, BatchSellTradesResponse, GaslessProperties, SimulatedGasFeeLimits, TokenAmountValues, Step, RefuelData, Quote, QuoteResponse, FeeData, TxData, Intent, IntentOrderLike, BitcoinTradeData, TronTradeData, BridgeControllerState, BridgeControllerAction, BridgeControllerActions, BridgeControllerEvents, BridgeControllerMessenger, FeatureFlagsPlatformConfig, TxFeeGasLimits, } from "./types.mjs";
-export type { BridgeControllerUpdateBridgeQuoteRequestParamsAction, BridgeControllerFetchQuotesAction, BridgeControllerStopPollingForQuotesAction, BridgeControllerSetLocationAction, BridgeControllerResetStateAction, BridgeControllerSetChainIntervalLengthAction, BridgeControllerTrackUnifiedSwapBridgeEventAction, BridgeControllerUpdateBatchSellTradesAction, } from "./bridge-controller-method-action-types.mjs";
+export type { BridgeControllerUpdateBridgeQuoteRequestParamsAction, BridgeControllerFetchQuotesAction, BridgeControllerStopPollingForQuotesAction, BridgeControllerSetLocationAction, BridgeControllerGetLocationAction, BridgeControllerResetStateAction, BridgeControllerSetChainIntervalLengthAction, BridgeControllerTrackUnifiedSwapBridgeEventAction, BridgeControllerUpdateBatchSellTradesAction, } from "./bridge-controller-method-action-types.mjs";
 export { AbortReason } from "./utils/metrics/constants.mjs";
 export { StatusTypes } from "./types.mjs";
 export { AssetType, SortOrder, ChainId, RequestStatus, type TokenFeature, type QuoteStreamCompleteData, type BridgeControllerGetStateAction, type BridgeControllerStateChangeEvent, } from "./types.mjs";
diff --git a/dist/utils/metrics/constants.cjs b/dist/utils/metrics/constants.cjs
index fff03ba56fd99dd5def2a103dd8e23ac852a0818..86c904883f9238784975061074d80e973d4623bf 100644
--- a/dist/utils/metrics/constants.cjs
+++ b/dist/utils/metrics/constants.cjs
@@ -52,6 +52,7 @@ var MetaMetricsSwapsEventSource;
     MetaMetricsSwapsEventSource["TokenView"] = "Token View";
     MetaMetricsSwapsEventSource["TrendingExplore"] = "Trending Explore";
     MetaMetricsSwapsEventSource["Rewards"] = "Rewards";
+    MetaMetricsSwapsEventSource["Unknown"] = "Unknown";
 })(MetaMetricsSwapsEventSource || (exports.MetaMetricsSwapsEventSource = MetaMetricsSwapsEventSource = {}));
 var InputAmountPreset;
 (function (InputAmountPreset) {
diff --git a/dist/utils/metrics/constants.d.cts b/dist/utils/metrics/constants.d.cts
index 42b2f77cd8197e664e632cc4d34662c2d37d64ca..0389e1ef6d42afe54ea712573dc514856dd50a80 100644
--- a/dist/utils/metrics/constants.d.cts
+++ b/dist/utils/metrics/constants.d.cts
@@ -43,7 +43,8 @@ export declare enum MetaMetricsSwapsEventSource {
     MainView = "Main View",
     TokenView = "Token View",
     TrendingExplore = "Trending Explore",
-    Rewards = "Rewards"
+    Rewards = "Rewards",
+    Unknown = "Unknown"
 }
 export declare enum InputAmountPreset {
     PERCENT_25 = "25%",
diff --git a/dist/utils/metrics/constants.d.mts b/dist/utils/metrics/constants.d.mts
index d5f922e0768bb9b4d460d1c1f4bad24bef02b495..ed1199dddac6d121da803c0a166c9b4a2e8e49ef 100644
--- a/dist/utils/metrics/constants.d.mts
+++ b/dist/utils/metrics/constants.d.mts
@@ -43,7 +43,8 @@ export declare enum MetaMetricsSwapsEventSource {
     MainView = "Main View",
     TokenView = "Token View",
     TrendingExplore = "Trending Explore",
-    Rewards = "Rewards"
+    Rewards = "Rewards",
+    Unknown = "Unknown"
 }
 export declare enum InputAmountPreset {
     PERCENT_25 = "25%",
diff --git a/dist/utils/metrics/constants.mjs b/dist/utils/metrics/constants.mjs
index 0d37eac89e0a8da0aba112e29cea64a6bf72f525..703f99d4fbaf0d2a614f3a9f85584d10a2e35c05 100644
--- a/dist/utils/metrics/constants.mjs
+++ b/dist/utils/metrics/constants.mjs
@@ -49,6 +49,7 @@ export var MetaMetricsSwapsEventSource;
     MetaMetricsSwapsEventSource["TokenView"] = "Token View";
     MetaMetricsSwapsEventSource["TrendingExplore"] = "Trending Explore";
     MetaMetricsSwapsEventSource["Rewards"] = "Rewards";
+    MetaMetricsSwapsEventSource["Unknown"] = "Unknown";
 })(MetaMetricsSwapsEventSource || (MetaMetricsSwapsEventSource = {}));
 export var InputAmountPreset;
 (function (InputAmountPreset) {
diff --git a/dist/utils/metrics/properties.cjs b/dist/utils/metrics/properties.cjs
index 377731f8c16da3a55ff15573b181482da8fc98ec..4d743ed0d2cf54392acb8c2ba20a68d083e6f763 100644
--- a/dist/utils/metrics/properties.cjs
+++ b/dist/utils/metrics/properties.cjs
@@ -46,8 +46,9 @@ exports.formatProviderLabel = formatProviderLabel;
  * @param tokenSecurityTypeDestination - The security classification of the destination token,
  * supplied by the client (e.g. from token security/scanning data). Pass `null` when no
  * security data is available for the selected destination token.
- * @returns The analytics request params derived from the quote request, minus token symbols
- * which the caller provides separately.
+ * @returns The analytics request params derived from the quote request. Token symbols are
+ * omitted because the quote request only stores addresses; use
+ * {@link getQuotesReceivedProperties} when building a `QuotesReceived` payload.
  */
 const getRequestParams = ({ srcChainId, destChainId, srcTokenAddress, destTokenAddress, }, tokenSecurityTypeDestination) => {
     // Fallback to ETH if srcChainId is not defined. This is ok since the clients default to Ethereum as the source chain
@@ -114,6 +115,8 @@ const getQuotesReceivedProperties = (activeQuote, warnings = [], isSubmittable =
             ? (0, exports.formatProviderLabel)(recommendedQuote.quote)
             : provider,
         provider,
+        token_symbol_source: activeQuote?.quote.srcAsset.symbol ?? '',
+        token_symbol_destination: activeQuote?.quote.destAsset.symbol ?? null,
         warnings,
         price_impact: Number(activeQuote?.quote.priceData?.priceImpact ?? 0),
         ...(hasSufficientGasForQuote !== undefined && {
diff --git a/dist/utils/metrics/properties.d.cts b/dist/utils/metrics/properties.d.cts
index 5ceb3355a14d417a52bae2d489b0dfe267cdc891..ae6375141d4a038ccd481819b51bc6dde055c44e 100644
--- a/dist/utils/metrics/properties.d.cts
+++ b/dist/utils/metrics/properties.d.cts
@@ -16,8 +16,9 @@ export declare const formatProviderLabel: ({ bridgeId, bridges, }: QuoteResponse
  * @param tokenSecurityTypeDestination - The security classification of the destination token,
  * supplied by the client (e.g. from token security/scanning data). Pass `null` when no
  * security data is available for the selected destination token.
- * @returns The analytics request params derived from the quote request, minus token symbols
- * which the caller provides separately.
+ * @returns The analytics request params derived from the quote request. Token symbols are
+ * omitted because the quote request only stores addresses; use
+ * {@link getQuotesReceivedProperties} when building a `QuotesReceived` payload.
  */
 export declare const getRequestParams: ({ srcChainId, destChainId, srcTokenAddress, destTokenAddress, }: Partial<GenericQuoteRequest>, tokenSecurityTypeDestination: string | null) => Omit<RequestParams, 'token_symbol_source' | 'token_symbol_destination'>;
 export declare const getAccountHardwareType: (selectedAccount?: AccountsControllerState['internalAccounts']['accounts'][string]) => AccountHardwareType;
@@ -40,6 +41,8 @@ export declare const getQuotesReceivedProperties: (activeQuote: null | (QuoteRes
     usd_balance_source: number;
     best_quote_provider: `${string}_${string}`;
     provider: `${string}_${string}`;
+    token_symbol_source: string;
+    token_symbol_destination: string | null;
     warnings: QuoteWarning[];
     price_impact: number;
 };
diff --git a/dist/utils/metrics/properties.d.mts b/dist/utils/metrics/properties.d.mts
index 862d81d76faeab0276ce9d9aadb9f26793029d02..a396855315b6c432ccbe655289cfd653a882b716 100644
--- a/dist/utils/metrics/properties.d.mts
+++ b/dist/utils/metrics/properties.d.mts
@@ -16,8 +16,9 @@ export declare const formatProviderLabel: ({ bridgeId, bridges, }: QuoteResponse
  * @param tokenSecurityTypeDestination - The security classification of the destination token,
  * supplied by the client (e.g. from token security/scanning data). Pass `null` when no
  * security data is available for the selected destination token.
- * @returns The analytics request params derived from the quote request, minus token symbols
- * which the caller provides separately.
+ * @returns The analytics request params derived from the quote request. Token symbols are
+ * omitted because the quote request only stores addresses; use
+ * {@link getQuotesReceivedProperties} when building a `QuotesReceived` payload.
  */
 export declare const getRequestParams: ({ srcChainId, destChainId, srcTokenAddress, destTokenAddress, }: Partial<GenericQuoteRequest>, tokenSecurityTypeDestination: string | null) => Omit<RequestParams, 'token_symbol_source' | 'token_symbol_destination'>;
 export declare const getAccountHardwareType: (selectedAccount?: AccountsControllerState['internalAccounts']['accounts'][string]) => AccountHardwareType;
@@ -40,6 +41,8 @@ export declare const getQuotesReceivedProperties: (activeQuote: null | (QuoteRes
     usd_balance_source: number;
     best_quote_provider: `${string}_${string}`;
     provider: `${string}_${string}`;
+    token_symbol_source: string;
+    token_symbol_destination: string | null;
     warnings: QuoteWarning[];
     price_impact: number;
 };
diff --git a/dist/utils/metrics/properties.mjs b/dist/utils/metrics/properties.mjs
index 23b45c295ea58206ebcea7b268b63bc0c6e61402..a92e4d0ef21d1c020f284f5e881d6986fb2d9d7c 100644
--- a/dist/utils/metrics/properties.mjs
+++ b/dist/utils/metrics/properties.mjs
@@ -40,8 +40,9 @@ export const formatProviderLabel = ({ bridgeId, bridges, }) => `${bridgeId}_${br
  * @param tokenSecurityTypeDestination - The security classification of the destination token,
  * supplied by the client (e.g. from token security/scanning data). Pass `null` when no
  * security data is available for the selected destination token.
- * @returns The analytics request params derived from the quote request, minus token symbols
- * which the caller provides separately.
+ * @returns The analytics request params derived from the quote request. Token symbols are
+ * omitted because the quote request only stores addresses; use
+ * {@link getQuotesReceivedProperties} when building a `QuotesReceived` payload.
  */
 export const getRequestParams = ({ srcChainId, destChainId, srcTokenAddress, destTokenAddress, }, tokenSecurityTypeDestination) => {
     // Fallback to ETH if srcChainId is not defined. This is ok since the clients default to Ethereum as the source chain
@@ -104,6 +105,8 @@ export const getQuotesReceivedProperties = (activeQuote, warnings = [], isSubmit
             ? formatProviderLabel(recommendedQuote.quote)
             : provider,
         provider,
+        token_symbol_source: activeQuote?.quote.srcAsset.symbol ?? '',
+        token_symbol_destination: activeQuote?.quote.destAsset.symbol ?? null,
         warnings,
         price_impact: Number(activeQuote?.quote.priceData?.priceImpact ?? 0),
         ...(hasSufficientGasForQuote !== undefined && {
diff --git a/dist/utils/metrics/types.d.cts b/dist/utils/metrics/types.d.cts
index 60158bb2246f253824629097bb18b84d15356e96..bcdf203d4e6290182385cf1e6d01732823680a17 100644
--- a/dist/utils/metrics/types.d.cts
+++ b/dist/utils/metrics/types.d.cts
@@ -83,7 +83,8 @@ type RequiredEventContextFromClientBase = {
         token_symbol_source: RequestParams['token_symbol_source'];
         token_symbol_destination: RequestParams['token_symbol_destination'];
     };
-    [UnifiedSwapBridgeEventName.QuotesReceived]: TradeData & {
+    [UnifiedSwapBridgeEventName.QuotesReceived]: TradeData &
+        Pick<RequestParams, 'token_symbol_source' | 'token_symbol_destination'> & {
         warnings: QuoteWarning[];
         best_quote_provider: QuoteFetchData['best_quote_provider'];
         price_impact: QuoteFetchData['price_impact'];
@@ -156,7 +157,7 @@ type RequiredEventContextFromClientBase = {
  * Properties that are required to be provided when trackUnifiedSwapBridgeEvent is called.
  * This combines the event-specific properties from RequiredEventContextFromClientBase
  * with an optional `location` property. When `location` is omitted, the controller
- * falls back to the value stored via `setLocation()` (defaults to MainView).
+ * falls back to the value stored via `setLocation()` (defaults to Unknown).
  *
  * `ab_tests` is the legacy field and `active_ab_tests` is the newer field.
  * Both are kept for a migration window and are treated as separate payloads.
diff --git a/dist/utils/metrics/types.d.mts b/dist/utils/metrics/types.d.mts
index 201d12ed74c4f3043ae82e55430ade2a1407af1f..3045efc02e2c25cc843ed4a690d935b6a7894368 100644
--- a/dist/utils/metrics/types.d.mts
+++ b/dist/utils/metrics/types.d.mts
@@ -83,7 +83,8 @@ type RequiredEventContextFromClientBase = {
         token_symbol_source: RequestParams['token_symbol_source'];
         token_symbol_destination: RequestParams['token_symbol_destination'];
     };
-    [UnifiedSwapBridgeEventName.QuotesReceived]: TradeData & {
+    [UnifiedSwapBridgeEventName.QuotesReceived]: TradeData &
+        Pick<RequestParams, 'token_symbol_source' | 'token_symbol_destination'> & {
         warnings: QuoteWarning[];
         best_quote_provider: QuoteFetchData['best_quote_provider'];
         price_impact: QuoteFetchData['price_impact'];
@@ -156,7 +157,7 @@ type RequiredEventContextFromClientBase = {
  * Properties that are required to be provided when trackUnifiedSwapBridgeEvent is called.
  * This combines the event-specific properties from RequiredEventContextFromClientBase
  * with an optional `location` property. When `location` is omitted, the controller
- * falls back to the value stored via `setLocation()` (defaults to MainView).
+ * falls back to the value stored via `setLocation()` (defaults to Unknown).
  *
  * `ab_tests` is the legacy field and `active_ab_tests` is the newer field.
  * Both are kept for a migration window and are treated as separate payloads.
