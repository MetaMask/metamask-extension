diff --git a/dist/defaults.cjs b/dist/defaults.cjs
index f2235110fad15dd99eb5b22d5c10116be40b8fdc..578efcd7a0e5743765ffd1fabba847fb91980474 100644
--- a/dist/defaults.cjs
+++ b/dist/defaults.cjs
@@ -26,18 +26,15 @@ const MUSD_METADATA = {
     decimals: 6,
 };
 /**
- * Harcoded metadata for USDC on Arc (5042/0x13b2).
- * USDC exists as both native (18 decimals) and ERC20 (6 decimals).
- * We choose to force-hide the native version to avoid double listing using
- * `CHAIN_IDS_WITH_NO_NATIVE_TOKEN`.
- * In the meantime, the code below force-shows USDC the ERC20 token.
+ * Arc's native USDC also exists as its own ERC20 (0x3600...); Accounts API
+ * resolves that identity without ever seeding native assetsInfo metadata.
  */
-const USDC_ON_ARC_ASSET_ID = 'eip155:5042/erc20:0x3600000000000000000000000000000000000000';
-const USDC_ON_ARC_METADATA = {
-    type: 'erc20',
+const ARC_NATIVE_ASSET_ID = 'eip155:5042/slip44:5042';
+const ARC_NATIVE_METADATA = {
+    type: 'native',
     symbol: 'USDC',
     name: 'USDC',
-    decimals: 6,
+    decimals: 18,
 };
 /**
  * Build the CAIP-19 asset ID for the mUSD ERC-20 token on a given EVM
@@ -63,7 +60,7 @@ exports.DEFAULT_TRACKED_ASSETS_BY_CHAIN = new Map([
     ['eip155:1', [musdAssetId('eip155:1')]],
     ['eip155:59144', [musdAssetId('eip155:59144')]],
     ['eip155:143', [musdAssetId('eip155:143')]],
-    ['eip155:5042', [USDC_ON_ARC_ASSET_ID]],
+    ['eip155:5042', [ARC_NATIVE_ASSET_ID]],
 ]);
 /**
  * Chains that have at least one default tracked asset. Useful for
@@ -79,7 +76,7 @@ exports.DEFAULT_ASSET_METADATA = new Map([
     [musdAssetId('eip155:1'), MUSD_METADATA],
     [musdAssetId('eip155:59144'), MUSD_METADATA],
     [musdAssetId('eip155:143'), MUSD_METADATA],
-    [USDC_ON_ARC_ASSET_ID, USDC_ON_ARC_METADATA],
+    [ARC_NATIVE_ASSET_ID, ARC_NATIVE_METADATA],
 ]);
 /**
  * Return the default tracked assets for a CAIP-2 chain id. Empty when
diff --git a/dist/defaults.mjs b/dist/defaults.mjs
index ccc5fb822dc28fb1f43422789e4a2e511b10f037..4d00a02e60e03227a7dfc439027838de426f57c3 100644
--- a/dist/defaults.mjs
+++ b/dist/defaults.mjs
@@ -23,18 +23,15 @@ const MUSD_METADATA = {
     decimals: 6,
 };
 /**
- * Harcoded metadata for USDC on Arc (5042/0x13b2).
- * USDC exists as both native (18 decimals) and ERC20 (6 decimals).
- * We choose to force-hide the native version to avoid double listing using
- * `CHAIN_IDS_WITH_NO_NATIVE_TOKEN`.
- * In the meantime, the code below force-shows USDC the ERC20 token.
+ * Arc's native USDC also exists as its own ERC20 (0x3600...); Accounts API
+ * resolves that identity without ever seeding native assetsInfo metadata.
  */
-const USDC_ON_ARC_ASSET_ID = 'eip155:5042/erc20:0x3600000000000000000000000000000000000000';
-const USDC_ON_ARC_METADATA = {
-    type: 'erc20',
+const ARC_NATIVE_ASSET_ID = 'eip155:5042/slip44:5042';
+const ARC_NATIVE_METADATA = {
+    type: 'native',
     symbol: 'USDC',
     name: 'USDC',
-    decimals: 6,
+    decimals: 18,
 };
 /**
  * Build the CAIP-19 asset ID for the mUSD ERC-20 token on a given EVM
@@ -60,7 +57,7 @@ export const DEFAULT_TRACKED_ASSETS_BY_CHAIN = new Map([
     ['eip155:1', [musdAssetId('eip155:1')]],
     ['eip155:59144', [musdAssetId('eip155:59144')]],
     ['eip155:143', [musdAssetId('eip155:143')]],
-    ['eip155:5042', [USDC_ON_ARC_ASSET_ID]],
+    ['eip155:5042', [ARC_NATIVE_ASSET_ID]],
 ]);
 /**
  * Chains that have at least one default tracked asset. Useful for
@@ -76,7 +73,7 @@ export const DEFAULT_ASSET_METADATA = new Map([
     [musdAssetId('eip155:1'), MUSD_METADATA],
     [musdAssetId('eip155:59144'), MUSD_METADATA],
     [musdAssetId('eip155:143'), MUSD_METADATA],
-    [USDC_ON_ARC_ASSET_ID, USDC_ON_ARC_METADATA],
+    [ARC_NATIVE_ASSET_ID, ARC_NATIVE_METADATA],
 ]);
 /**
  * Return the default tracked assets for a CAIP-2 chain id. Empty when
