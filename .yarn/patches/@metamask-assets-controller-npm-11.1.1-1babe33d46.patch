diff --git a/dist/selectors/balance.cjs b/dist/selectors/balance.cjs
index 0bf591b98908a85fd949b5f76ed101c07d2150ee..16b487c4315b4a0b52c10bc2e582545f0b5b9a42 100644
--- a/dist/selectors/balance.cjs
+++ b/dist/selectors/balance.cjs
@@ -23,25 +23,6 @@ const toBigNumberOrZero = (value) => {
     const parsed = new bignumber_js_1.default(value);
     return parsed.isNaN() || !parsed.isFinite() ? new bignumber_js_1.default(0) : parsed;
 };
-/**
- * When state contains raw base units (e.g. SPORE "16201541034.639288184" with 9 decimals),
- * scale to human (16.2) before pricing. Only scale when amount >= 10^decimals so we don't
- * double-scale amounts that are already human (e.g. "1", "0.227285").
- *
- * @param amountRaw - Amount from state (may be raw or human).
- * @param decimals - Token decimals from metadata.
- * @returns Human amount for display and fiat (amountRaw or amountRaw / 10^decimals).
- */
-const scaleToHumanIfRaw = (amountRaw, decimals) => {
-    if (decimals <= 0) {
-        return amountRaw;
-    }
-    const scale = new bignumber_js_1.default(10).pow(decimals);
-    if (amountRaw.lt(scale)) {
-        return amountRaw;
-    }
-    return amountRaw.dividedBy(scale);
-};
 const getPriceDatumFast = (assetsPrice, assetId) => {
     const raw = assetsPrice?.[assetId];
     if (!raw || typeof raw !== 'object') {
@@ -123,11 +104,12 @@ function mergeBalancesIntoMap(args) {
             continue;
         }
         const amountStr = getAmountFromBalance(accountBalances[typedAssetId]);
-        let amountBn = toBigNumberOrZero(amountStr);
+        // Amounts in state are already human-readable (data sources convert raw
+        // base units on write), so use them as-is. Guessing raw vs human by
+        // magnitude corrupts legitimately large balances (e.g. a 54B-token
+        // balance with 9 decimals would be wrongly divided by 10^9).
+        const amountBn = toBigNumberOrZero(amountStr);
         const meta = metadata[typedAssetId];
-        if (meta?.decimals !== undefined) {
-            amountBn = scaleToHumanIfRaw(amountBn, meta.decimals);
-        }
         if (amountBn.isZero()) {
             continue; // skip zeros early to reduce map pressure
         }
diff --git a/dist/selectors/balance.mjs b/dist/selectors/balance.mjs
index ec9437b0674b780af6ff7e8a0cd919c259bdc333..0f14910ee6bc070d525908a0f96125c5bd51b102 100644
--- a/dist/selectors/balance.mjs
+++ b/dist/selectors/balance.mjs
@@ -17,25 +17,6 @@ const toBigNumberOrZero = (value) => {
     const parsed = new BigNumberJS(value);
     return parsed.isNaN() || !parsed.isFinite() ? new BigNumberJS(0) : parsed;
 };
-/**
- * When state contains raw base units (e.g. SPORE "16201541034.639288184" with 9 decimals),
- * scale to human (16.2) before pricing. Only scale when amount >= 10^decimals so we don't
- * double-scale amounts that are already human (e.g. "1", "0.227285").
- *
- * @param amountRaw - Amount from state (may be raw or human).
- * @param decimals - Token decimals from metadata.
- * @returns Human amount for display and fiat (amountRaw or amountRaw / 10^decimals).
- */
-const scaleToHumanIfRaw = (amountRaw, decimals) => {
-    if (decimals <= 0) {
-        return amountRaw;
-    }
-    const scale = new BigNumberJS(10).pow(decimals);
-    if (amountRaw.lt(scale)) {
-        return amountRaw;
-    }
-    return amountRaw.dividedBy(scale);
-};
 const getPriceDatumFast = (assetsPrice, assetId) => {
     const raw = assetsPrice?.[assetId];
     if (!raw || typeof raw !== 'object') {
@@ -117,11 +98,12 @@ function mergeBalancesIntoMap(args) {
             continue;
         }
         const amountStr = getAmountFromBalance(accountBalances[typedAssetId]);
-        let amountBn = toBigNumberOrZero(amountStr);
+        // Amounts in state are already human-readable (data sources convert raw
+        // base units on write), so use them as-is. Guessing raw vs human by
+        // magnitude corrupts legitimately large balances (e.g. a 54B-token
+        // balance with 9 decimals would be wrongly divided by 10^9).
+        const amountBn = toBigNumberOrZero(amountStr);
         const meta = metadata[typedAssetId];
-        if (meta?.decimals !== undefined) {
-            amountBn = scaleToHumanIfRaw(amountBn, meta.decimals);
-        }
         if (amountBn.isZero()) {
             continue; // skip zeros early to reduce map pressure
         }
