diff --git a/dist/createUIQueryClient.cjs b/dist/createUIQueryClient.cjs
index 98408a985d04a2c9e6dd4ba18f2866f125129bb6..2d08e6709d37b8f6f0c01eec0fd4340498a1e663 100644
--- a/dist/createUIQueryClient.cjs
+++ b/dist/createUIQueryClient.cjs
@@ -82,10 +82,9 @@ function createUIQueryClient(dataServices, messenger, config = {}) {
         }
     });
     // Override invalidateQueries to ensure the data service is invalidated as well.
+    // TanStack Query v5 dropped parseFilterArgs; invalidateQueries takes filters + options.
     const originalInvalidate = client.invalidateQueries.bind(client);
-    // This function is defined in this way to have full support for all function overloads.
-    client.invalidateQueries = async (arg1, arg2, arg3) => {
-        const [filters, options] = (0, query_core_1.parseFilterArgs)(arg1, arg2, arg3);
+    client.invalidateQueries = async (filters, options) => {
         const queries = client.getQueryCache().findAll(filters);
         const services = [
             ...new Set(queries.map((query) => parseQueryKey(query.queryKey))),
diff --git a/dist/createUIQueryClient.mjs b/dist/createUIQueryClient.mjs
index 2bd20c19ba96302d5babab9a3205d923689ee1d4..ef2e9c573039f42ec9aa7ccfe944bbffff87f2a8 100644
--- a/dist/createUIQueryClient.mjs
+++ b/dist/createUIQueryClient.mjs
@@ -1,5 +1,5 @@
 import { assert } from "@metamask/utils";
-import { hydrate, QueryClient, parseFilterArgs } from "@tanstack/query-core";
+import { hydrate, QueryClient } from "@tanstack/query-core";
 /**
  * Create a QueryClient queries and subscribes to data services using the messenger.
  *
@@ -79,10 +79,9 @@ export function createUIQueryClient(dataServices, messenger, config = {}) {
         }
     });
     // Override invalidateQueries to ensure the data service is invalidated as well.
+    // TanStack Query v5 dropped parseFilterArgs; invalidateQueries takes filters + options.
     const originalInvalidate = client.invalidateQueries.bind(client);
-    // This function is defined in this way to have full support for all function overloads.
-    client.invalidateQueries = async (arg1, arg2, arg3) => {
-        const [filters, options] = parseFilterArgs(arg1, arg2, arg3);
+    client.invalidateQueries = async (filters, options) => {
         const queries = client.getQueryCache().findAll(filters);
         const services = [
             ...new Set(queries.map((query) => parseQueryKey(query.queryKey))),
diff --git a/package.json b/package.json
index cb523dea7faed1f5fc1c38c9156a15f07683a6d5..7a287c3666aada8a8306c5903d1fd7ef8dd5b0da 100644
--- a/package.json
+++ b/package.json
@@ -49,8 +49,8 @@
   "dependencies": {
     "@metamask/base-data-service": "^0.1.0",
     "@metamask/utils": "^11.9.0",
-    "@tanstack/query-core": "^4.43.0",
-    "@tanstack/react-query": "^4.43.0"
+    "@tanstack/query-core": "^5.62.16",
+    "@tanstack/react-query": "^5.62.16"
   },
   "devDependencies": {
     "@metamask/auto-changelog": "^3.4.4",
