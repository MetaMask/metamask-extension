diff --git a/dist/strategy/across/across-quotes.cjs b/dist/strategy/across/across-quotes.cjs
index c031383612d32a9ad06d2bf778206d2295c70255..84c438cd70a773fc08fc69615f3c29845f2407aa 100644
--- a/dist/strategy/across/across-quotes.cjs
+++ b/dist/strategy/across/across-quotes.cjs
@@ -43,7 +43,7 @@ async function getAcrossQuotes(request) {
 }
 exports.getAcrossQuotes = getAcrossQuotes;
 async function getSingleQuote(request, fullRequest) {
-    const { messenger, transaction } = fullRequest;
+    const { messenger, signal, transaction } = fullRequest;
     const normalizedRequest = (0, perps_1.normalizeAcrossRequest)(request, transaction.type);
     const { from, isMaxAmount, sourceChainId, sourceTokenAddress, sourceTokenAmount, targetAmountMinimum, targetChainId, targetTokenAddress, } = normalizedRequest;
     const config = (0, feature_flags_1.getPayStrategiesConfig)(messenger);
@@ -61,6 +61,7 @@ async function getSingleQuote(request, fullRequest) {
         originChainId: sourceChainId,
         outputToken: targetTokenAddress,
         recipient: destination.recipient,
+        signal,
         slippage: slippageDecimal,
         tradeType,
     });
@@ -74,7 +75,7 @@ async function getSingleQuote(request, fullRequest) {
     return await normalizeQuote(originalQuote, normalizedRequest, fullRequest);
 }
 async function requestAcrossApproval(request) {
-    const { actions, amount, apiBase, depositor, destinationChainId, inputToken, originChainId, outputToken, recipient, slippage, tradeType, } = request;
+    const { actions, amount, apiBase, depositor, destinationChainId, inputToken, originChainId, outputToken, recipient, signal, slippage, tradeType, } = request;
     const params = new URLSearchParams();
     params.set('tradeType', tradeType);
     params.set('amount', amount);
@@ -96,6 +97,7 @@ async function requestAcrossApproval(request) {
             'Content-Type': 'application/json',
         },
         method: 'POST',
+        signal,
     };
     const response = await (0, controller_utils_1.successfulFetch)(url, options);
     return (await response.json());
diff --git a/dist/strategy/across/across-quotes.mjs b/dist/strategy/across/across-quotes.mjs
index db6a3455614e374fa4f91b361d32d4e6dd0e7409..e8059de63d8c4ccd6a483ecf5fac52c0d8a61443 100644
--- a/dist/strategy/across/across-quotes.mjs
+++ b/dist/strategy/across/across-quotes.mjs
@@ -39,7 +39,7 @@ export async function getAcrossQuotes(request) {
     }
 }
 async function getSingleQuote(request, fullRequest) {
-    const { messenger, transaction } = fullRequest;
+    const { messenger, signal, transaction } = fullRequest;
     const normalizedRequest = normalizeAcrossRequest(request, transaction.type);
     const { from, isMaxAmount, sourceChainId, sourceTokenAddress, sourceTokenAmount, targetAmountMinimum, targetChainId, targetTokenAddress, } = normalizedRequest;
     const config = getPayStrategiesConfig(messenger);
@@ -57,6 +57,7 @@ async function getSingleQuote(request, fullRequest) {
         originChainId: sourceChainId,
         outputToken: targetTokenAddress,
         recipient: destination.recipient,
+        signal,
         slippage: slippageDecimal,
         tradeType,
     });
@@ -70,7 +71,7 @@ async function getSingleQuote(request, fullRequest) {
     return await normalizeQuote(originalQuote, normalizedRequest, fullRequest);
 }
 async function requestAcrossApproval(request) {
-    const { actions, amount, apiBase, depositor, destinationChainId, inputToken, originChainId, outputToken, recipient, slippage, tradeType, } = request;
+    const { actions, amount, apiBase, depositor, destinationChainId, inputToken, originChainId, outputToken, recipient, signal, slippage, tradeType, } = request;
     const params = new URLSearchParams();
     params.set('tradeType', tradeType);
     params.set('amount', amount);
@@ -92,6 +93,7 @@ async function requestAcrossApproval(request) {
             'Content-Type': 'application/json',
         },
         method: 'POST',
+        signal,
     };
     const response = await successfulFetch(url, options);
     return (await response.json());
diff --git a/dist/strategy/relay/relay-api.cjs b/dist/strategy/relay/relay-api.cjs
index f22ab75834811798534560b3526b71cd59dac221..8f4e833d42ffef183d4a946993a77e254cfd0021 100644
--- a/dist/strategy/relay/relay-api.cjs
+++ b/dist/strategy/relay/relay-api.cjs
@@ -11,12 +11,13 @@ const constants_1 = require("./constants.cjs");
  * @param body - Quote request parameters.
  * @returns The Relay quote with the request attached.
  */
-async function fetchRelayQuote(messenger, body) {
+async function fetchRelayQuote(messenger, body, signal) {
     const { relayQuoteUrl } = (0, feature_flags_1.getFeatureFlags)(messenger);
     const response = await (0, controller_utils_1.successfulFetch)(relayQuoteUrl, {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify(body),
+        signal,
     });
     const quote = (await response.json());
     quote.request = body;
diff --git a/dist/strategy/relay/relay-api.d.cts b/dist/strategy/relay/relay-api.d.cts
index 33b947f3d77ef09454f0b901c110b2347bdad824..703277b6c22bf992083be7c32a4514996459e756 100644
--- a/dist/strategy/relay/relay-api.d.cts
+++ b/dist/strategy/relay/relay-api.d.cts
@@ -7,7 +7,7 @@ import type { RelayExecuteRequest, RelayExecuteResponse, RelayQuote, RelayQuoteR
  * @param body - Quote request parameters.
  * @returns The Relay quote with the request attached.
  */
-export declare function fetchRelayQuote(messenger: TransactionPayControllerMessenger, body: RelayQuoteRequest): Promise<RelayQuote>;
+export declare function fetchRelayQuote(messenger: TransactionPayControllerMessenger, body: RelayQuoteRequest, signal?: AbortSignal): Promise<RelayQuote>;
 /**
  * Submit a gasless transaction via the Relay /execute endpoint.
  *
diff --git a/dist/strategy/relay/relay-api.d.mts b/dist/strategy/relay/relay-api.d.mts
index ad8163d97e10a449b0467f2aa6ecb185a984342c..7d49237e52b97fcce3da53f16b1775e9de449805 100644
--- a/dist/strategy/relay/relay-api.d.mts
+++ b/dist/strategy/relay/relay-api.d.mts
@@ -7,7 +7,7 @@ import type { RelayExecuteRequest, RelayExecuteResponse, RelayQuote, RelayQuoteR
  * @param body - Quote request parameters.
  * @returns The Relay quote with the request attached.
  */
-export declare function fetchRelayQuote(messenger: TransactionPayControllerMessenger, body: RelayQuoteRequest): Promise<RelayQuote>;
+export declare function fetchRelayQuote(messenger: TransactionPayControllerMessenger, body: RelayQuoteRequest, signal?: AbortSignal): Promise<RelayQuote>;
 /**
  * Submit a gasless transaction via the Relay /execute endpoint.
  *
diff --git a/dist/strategy/relay/relay-api.mjs b/dist/strategy/relay/relay-api.mjs
index da3bcff7bdf0e1f33be0307bb51c6e152f54693d..28bc4957828cf5d9f8c255a76c54cc279c3bad57 100644
--- a/dist/strategy/relay/relay-api.mjs
+++ b/dist/strategy/relay/relay-api.mjs
@@ -8,12 +8,13 @@ import { RELAY_STATUS_URL } from "./constants.mjs";
  * @param body - Quote request parameters.
  * @returns The Relay quote with the request attached.
  */
-export async function fetchRelayQuote(messenger, body) {
+export async function fetchRelayQuote(messenger, body, signal) {
     const { relayQuoteUrl } = getFeatureFlags(messenger);
     const response = await successfulFetch(relayQuoteUrl, {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify(body),
+        signal,
     });
     const quote = (await response.json());
     quote.request = body;
diff --git a/dist/strategy/relay/relay-quotes.cjs b/dist/strategy/relay/relay-quotes.cjs
index 0c7d6d00b732eaad930a009f4de8fd263abbf20b..86dbcde6841101fafbf91f4e7c454b210e47293d 100644
--- a/dist/strategy/relay/relay-quotes.cjs
+++ b/dist/strategy/relay/relay-quotes.cjs
@@ -106,7 +106,7 @@ async function getQuoteWithPostQuoteGasHandling(request, fullRequest) {
  * @returns  Single quote.
  */
 async function getSingleQuote(request, fullRequest) {
-    const { messenger, transaction } = fullRequest;
+    const { messenger, signal, transaction, } = fullRequest;
     const { from, isMaxAmount, sourceChainId, sourceTokenAddress, sourceTokenAmount, targetAmountMinimum, targetChainId, targetTokenAddress, } = request;
     const slippageDecimal = (0, feature_flags_1.getSlippage)(messenger, sourceChainId, sourceTokenAddress);
     const slippageTolerance = new bignumber_js_1.BigNumber(slippageDecimal * 100 * 100).toFixed(0);
@@ -144,7 +144,7 @@ async function getSingleQuote(request, fullRequest) {
             body.refundTo = request.refundTo;
         }
         log('Request body', body);
-        const quote = await (0, relay_api_1.fetchRelayQuote)(messenger, body);
+        const quote = await (0, relay_api_1.fetchRelayQuote)(messenger, body, signal);
         log('Fetched relay quote', quote);
         return await normalizeQuote(quote, request, fullRequest);
     }
diff --git a/dist/strategy/relay/relay-quotes.mjs b/dist/strategy/relay/relay-quotes.mjs
index 5f4564e52761e2d0ed5abb7abfc88ac49644aca2..56c98f72ae64c7df8b31ebf387b40fa8e42f055d 100644
--- a/dist/strategy/relay/relay-quotes.mjs
+++ b/dist/strategy/relay/relay-quotes.mjs
@@ -102,7 +102,7 @@ async function getQuoteWithPostQuoteGasHandling(request, fullRequest) {
  * @returns  Single quote.
  */
 async function getSingleQuote(request, fullRequest) {
-    const { messenger, transaction } = fullRequest;
+    const { messenger, signal, transaction, } = fullRequest;
     const { from, isMaxAmount, sourceChainId, sourceTokenAddress, sourceTokenAmount, targetAmountMinimum, targetChainId, targetTokenAddress, } = request;
     const slippageDecimal = getSlippage(messenger, sourceChainId, sourceTokenAddress);
     const slippageTolerance = new BigNumber(slippageDecimal * 100 * 100).toFixed(0);
@@ -140,7 +140,7 @@ async function getSingleQuote(request, fullRequest) {
             body.refundTo = request.refundTo;
         }
         log('Request body', body);
-        const quote = await fetchRelayQuote(messenger, body);
+        const quote = await fetchRelayQuote(messenger, body, signal);
         log('Fetched relay quote', quote);
         return await normalizeQuote(quote, request, fullRequest);
     }
diff --git a/dist/types.d.cts b/dist/types.d.cts
index 4eb7f78bd0e6e6c5edc4276bd5e7847b788dda9f..54aea578b2309deacf8dfc2a5073c4124312ef8b 100644
--- a/dist/types.d.cts
+++ b/dist/types.d.cts
@@ -279,6 +279,12 @@ export type PayStrategyGetQuotesRequest = {
     messenger: TransactionPayControllerMessenger;
     /** Quote requests for required tokens. */
     requests: QuoteRequest[];
+    /**
+     * Signal that aborts when a newer quote request supersedes this one.
+     * Strategies that perform their own network IO should forward this to
+     * their fetch calls so cancelled requests release network resources.
+     */
+    signal?: AbortSignal;
     /** Metadata of the original target transaction. */
     transaction: TransactionMeta;
 };
@@ -299,6 +305,8 @@ export type PayStrategyGetBatchRequest<OriginalQuote> = {
     messenger: TransactionPayControllerMessenger;
     /** Quotes for required tokens. */
     quotes: TransactionPayQuote<OriginalQuote>[];
+    /** Signal that aborts when a newer quote request supersedes this one. */
+    signal?: AbortSignal;
 };
 /** Request to get refresh interval for a specific strategy. */
 export type PayStrategyGetRefreshIntervalRequest = {
diff --git a/dist/types.d.mts b/dist/types.d.mts
index 5e6ea047f326e10a5af7768656d5a24fcd50716e..2ceec5269e436b35332a5b54b1de9c1b36a2b00e 100644
--- a/dist/types.d.mts
+++ b/dist/types.d.mts
@@ -279,6 +279,12 @@ export type PayStrategyGetQuotesRequest = {
     messenger: TransactionPayControllerMessenger;
     /** Quote requests for required tokens. */
     requests: QuoteRequest[];
+    /**
+     * Signal that aborts when a newer quote request supersedes this one.
+     * Strategies that perform their own network IO should forward this to
+     * their fetch calls so cancelled requests release network resources.
+     */
+    signal?: AbortSignal;
     /** Metadata of the original target transaction. */
     transaction: TransactionMeta;
 };
@@ -299,6 +305,8 @@ export type PayStrategyGetBatchRequest<OriginalQuote> = {
     messenger: TransactionPayControllerMessenger;
     /** Quotes for required tokens. */
     quotes: TransactionPayQuote<OriginalQuote>[];
+    /** Signal that aborts when a newer quote request supersedes this one. */
+    signal?: AbortSignal;
 };
 /** Request to get refresh interval for a specific strategy. */
 export type PayStrategyGetRefreshIntervalRequest = {
diff --git a/dist/utils/quotes.cjs b/dist/utils/quotes.cjs
index 3b6afede261ee2cf1e63dd860acc757dd5d64a47..4ec3fc2a22e144ae4feadfab765c2805345c0e94 100644
--- a/dist/utils/quotes.cjs
+++ b/dist/utils/quotes.cjs
@@ -11,6 +11,7 @@ const totals_1 = require("./totals.cjs");
 const transaction_1 = require("./transaction.cjs");
 const DEFAULT_REFRESH_INTERVAL = 30 * 1000; // 30 Seconds
 const log = (0, utils_1.createModuleLogger)(logger_1.projectLogger, 'quotes');
+const inFlightQuoteRequests = new Map();
 /**
  * Update the quotes for a specific transaction.
  *
@@ -29,6 +30,8 @@ async function updateQuotes(request) {
     log('Updating quotes', { transactionId });
     const { isMaxAmount, isPostQuote, isHyperliquidSource, paymentToken: originalPaymentToken, refundTo, sourceAmounts, tokens, } = transactionData;
     const from = transaction.txParams.from;
+    const controller = abortPreviousAndCreateController(transactionId);
+    const { signal } = controller;
     updateTransactionData(transactionId, (data) => {
         data.isLoading = true;
     });
@@ -37,9 +40,14 @@ async function updateQuotes(request) {
             from,
             messenger,
             paymentToken: originalPaymentToken,
+            signal,
             transactionId,
             updateTransactionData,
         });
+        if (signal.aborted) {
+            log('Quote request aborted before building requests', { transactionId });
+            return false;
+        }
         const requests = buildQuoteRequests({
             from,
             isMaxAmount: isMaxAmount ?? false,
@@ -51,7 +59,11 @@ async function updateQuotes(request) {
             tokens,
             transactionId,
         });
-        const { batchTransactions, quotes } = await getQuotes(transaction, requests, getStrategies, messenger, transactionData.fiatPayment?.selectedPaymentMethodId);
+        const { batchTransactions, quotes } = await getQuotes(transaction, requests, getStrategies, messenger, transactionData.fiatPayment?.selectedPaymentMethodId, signal);
+        if (signal.aborted) {
+            log('Quote request aborted before persisting results', { transactionId });
+            return false;
+        }
         const totals = (0, totals_1.calculateTotals)({
             isMaxAmount,
             messenger,
@@ -74,10 +86,20 @@ async function updateQuotes(request) {
             data.totals = totals;
         });
     }
+    catch (error) {
+        if (signal.aborted) {
+            log('Quote request aborted', { transactionId, reason: signal.reason });
+            return false;
+        }
+        throw error;
+    }
     finally {
-        updateTransactionData(transactionId, (data) => {
-            data.isLoading = false;
-        });
+        if (!signal.aborted) {
+            updateTransactionData(transactionId, (data) => {
+                data.isLoading = false;
+            });
+        }
+        clearControllerIfCurrent(transactionId, controller);
     }
     return true;
 }
@@ -154,6 +176,21 @@ async function refreshQuotes(messenger, updateTransactionData, getStrategies) {
     }
 }
 exports.refreshQuotes = refreshQuotes;
+function abortPreviousAndCreateController(transactionId) {
+    const previous = inFlightQuoteRequests.get(transactionId);
+    if (previous && !previous.signal.aborted) {
+        log('Aborting previous quote request', { transactionId });
+        previous.abort(new Error('Superseded by newer quote request'));
+    }
+    const controller = new AbortController();
+    inFlightQuoteRequests.set(transactionId, controller);
+    return controller;
+}
+function clearControllerIfCurrent(transactionId, controller) {
+    if (inFlightQuoteRequests.get(transactionId) === controller) {
+        inFlightQuoteRequests.delete(transactionId);
+    }
+}
 /**
  * Build quote requests required to retrieve quotes.
  *
@@ -251,7 +288,7 @@ function buildPostQuoteRequests({ from, isMaxAmount, isHyperliquidSource, destin
     // Multiple token support would require multiple quotes for each required token.
     return [request];
 }
-async function refreshPaymentTokenBalance({ from, messenger, paymentToken, transactionId, updateTransactionData, }) {
+async function refreshPaymentTokenBalance({ from, messenger, paymentToken, signal, transactionId, updateTransactionData, }) {
     if (!paymentToken) {
         return undefined;
     }
@@ -261,6 +298,10 @@ async function refreshPaymentTokenBalance({ from, messenger, paymentToken, trans
             return paymentToken;
         }
         const liveBalance = await (0, token_1.getLiveTokenBalance)(messenger, from, paymentToken.chainId, paymentToken.address);
+        if (signal.aborted) {
+            log('Payment token balance refresh aborted', { transactionId });
+            return paymentToken;
+        }
         const { raw: balanceRaw, human: balanceHuman, usd: balanceUsd, fiat: balanceFiat, } = (0, token_1.computeTokenAmounts)(liveBalance, paymentToken.decimals, fiatRates);
         const updatedToken = {
             ...paymentToken,
@@ -290,7 +331,7 @@ async function refreshPaymentTokenBalance({ from, messenger, paymentToken, trans
  * @param fiatPaymentMethod - Selected fiat payment method ID, if applicable.
  * @returns An object containing batch transactions and quotes.
  */
-async function getQuotes(transaction, requests, getStrategies, messenger, fiatPaymentMethod) {
+async function getQuotes(transaction, requests, getStrategies, messenger, fiatPaymentMethod, signal) {
     const { id: transactionId } = transaction;
     const strategies = (0, strategy_1.getStrategiesByName)(getStrategies(transaction), (strategyName) => {
         log('Skipping unknown strategy', {
@@ -308,6 +349,7 @@ async function getQuotes(transaction, requests, getStrategies, messenger, fiatPa
         fiatPaymentMethod,
         messenger,
         requests,
+        signal,
         transaction,
     };
     for (const { name, strategy } of strategies) {
@@ -329,6 +371,7 @@ async function getQuotes(transaction, requests, getStrategies, messenger, fiatPa
                 ? await strategy.getBatchTransactions({
                     messenger,
                     quotes,
+                    signal,
                 })
                 : [];
             log('Batch transactions', { transactionId, batchTransactions });
@@ -338,6 +381,9 @@ async function getQuotes(transaction, requests, getStrategies, messenger, fiatPa
             };
         }
         catch (error) {
+            if (signal?.aborted) {
+                throw error;
+            }
             log('Strategy failed, trying next', {
                 error,
                 strategy: name,
diff --git a/dist/utils/quotes.mjs b/dist/utils/quotes.mjs
index 34e5a4cf230917bceabfb141fddd1209d532b421..73819583c6f13ff85db399c5b1940ca4afe72af0 100644
--- a/dist/utils/quotes.mjs
+++ b/dist/utils/quotes.mjs
@@ -8,6 +8,7 @@ import { calculateTotals } from "./totals.mjs";
 import { getTransaction, updateTransaction } from "./transaction.mjs";
 const DEFAULT_REFRESH_INTERVAL = 30 * 1000; // 30 Seconds
 const log = createModuleLogger(projectLogger, 'quotes');
+const inFlightQuoteRequests = new Map();
 /**
  * Update the quotes for a specific transaction.
  *
@@ -26,6 +27,8 @@ export async function updateQuotes(request) {
     log('Updating quotes', { transactionId });
     const { isMaxAmount, isPostQuote, isHyperliquidSource, paymentToken: originalPaymentToken, refundTo, sourceAmounts, tokens, } = transactionData;
     const from = transaction.txParams.from;
+    const controller = abortPreviousAndCreateController(transactionId);
+    const { signal } = controller;
     updateTransactionData(transactionId, (data) => {
         data.isLoading = true;
     });
@@ -34,9 +37,14 @@ export async function updateQuotes(request) {
             from,
             messenger,
             paymentToken: originalPaymentToken,
+            signal,
             transactionId,
             updateTransactionData,
         });
+        if (signal.aborted) {
+            log('Quote request aborted before building requests', { transactionId });
+            return false;
+        }
         const requests = buildQuoteRequests({
             from,
             isMaxAmount: isMaxAmount ?? false,
@@ -48,7 +56,11 @@ export async function updateQuotes(request) {
             tokens,
             transactionId,
         });
-        const { batchTransactions, quotes } = await getQuotes(transaction, requests, getStrategies, messenger, transactionData.fiatPayment?.selectedPaymentMethodId);
+        const { batchTransactions, quotes } = await getQuotes(transaction, requests, getStrategies, messenger, transactionData.fiatPayment?.selectedPaymentMethodId, signal);
+        if (signal.aborted) {
+            log('Quote request aborted before persisting results', { transactionId });
+            return false;
+        }
         const totals = calculateTotals({
             isMaxAmount,
             messenger,
@@ -71,10 +83,20 @@ export async function updateQuotes(request) {
             data.totals = totals;
         });
     }
+    catch (error) {
+        if (signal.aborted) {
+            log('Quote request aborted', { transactionId, reason: signal.reason });
+            return false;
+        }
+        throw error;
+    }
     finally {
-        updateTransactionData(transactionId, (data) => {
-            data.isLoading = false;
-        });
+        if (!signal.aborted) {
+            updateTransactionData(transactionId, (data) => {
+                data.isLoading = false;
+            });
+        }
+        clearControllerIfCurrent(transactionId, controller);
     }
     return true;
 }
@@ -149,6 +171,21 @@ export async function refreshQuotes(messenger, updateTransactionData, getStrateg
         }
     }
 }
+function abortPreviousAndCreateController(transactionId) {
+    const previous = inFlightQuoteRequests.get(transactionId);
+    if (previous && !previous.signal.aborted) {
+        log('Aborting previous quote request', { transactionId });
+        previous.abort(new Error('Superseded by newer quote request'));
+    }
+    const controller = new AbortController();
+    inFlightQuoteRequests.set(transactionId, controller);
+    return controller;
+}
+function clearControllerIfCurrent(transactionId, controller) {
+    if (inFlightQuoteRequests.get(transactionId) === controller) {
+        inFlightQuoteRequests.delete(transactionId);
+    }
+}
 /**
  * Build quote requests required to retrieve quotes.
  *
@@ -246,7 +283,7 @@ function buildPostQuoteRequests({ from, isMaxAmount, isHyperliquidSource, destin
     // Multiple token support would require multiple quotes for each required token.
     return [request];
 }
-async function refreshPaymentTokenBalance({ from, messenger, paymentToken, transactionId, updateTransactionData, }) {
+async function refreshPaymentTokenBalance({ from, messenger, paymentToken, signal, transactionId, updateTransactionData, }) {
     if (!paymentToken) {
         return undefined;
     }
@@ -256,6 +293,10 @@ async function refreshPaymentTokenBalance({ from, messenger, paymentToken, trans
             return paymentToken;
         }
         const liveBalance = await getLiveTokenBalance(messenger, from, paymentToken.chainId, paymentToken.address);
+        if (signal.aborted) {
+            log('Payment token balance refresh aborted', { transactionId });
+            return paymentToken;
+        }
         const { raw: balanceRaw, human: balanceHuman, usd: balanceUsd, fiat: balanceFiat, } = computeTokenAmounts(liveBalance, paymentToken.decimals, fiatRates);
         const updatedToken = {
             ...paymentToken,
@@ -285,7 +326,7 @@ async function refreshPaymentTokenBalance({ from, messenger, paymentToken, trans
  * @param fiatPaymentMethod - Selected fiat payment method ID, if applicable.
  * @returns An object containing batch transactions and quotes.
  */
-async function getQuotes(transaction, requests, getStrategies, messenger, fiatPaymentMethod) {
+async function getQuotes(transaction, requests, getStrategies, messenger, fiatPaymentMethod, signal) {
     const { id: transactionId } = transaction;
     const strategies = getStrategiesByName(getStrategies(transaction), (strategyName) => {
         log('Skipping unknown strategy', {
@@ -303,6 +344,7 @@ async function getQuotes(transaction, requests, getStrategies, messenger, fiatPa
         fiatPaymentMethod,
         messenger,
         requests,
+        signal,
         transaction,
     };
     for (const { name, strategy } of strategies) {
@@ -324,6 +366,7 @@ async function getQuotes(transaction, requests, getStrategies, messenger, fiatPa
                 ? await strategy.getBatchTransactions({
                     messenger,
                     quotes,
+                    signal,
                 })
                 : [];
             log('Batch transactions', { transactionId, batchTransactions });
@@ -333,6 +376,9 @@ async function getQuotes(transaction, requests, getStrategies, messenger, fiatPa
             };
         }
         catch (error) {
+            if (signal?.aborted) {
+                throw error;
+            }
             log('Strategy failed, trying next', {
                 error,
                 strategy: name,
