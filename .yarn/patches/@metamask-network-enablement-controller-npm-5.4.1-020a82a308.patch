diff --git a/dist/NetworkEnablementController.cjs b/dist/NetworkEnablementController.cjs
index 3f071f8a148e4d92ff319608ae124f99b00c3922..1168c358c48121f7f8b301f7928a2f62683538c9 100644
--- a/dist/NetworkEnablementController.cjs
+++ b/dist/NetworkEnablementController.cjs
@@ -408,6 +408,28 @@ class NetworkEnablementController extends base_controller_1.BaseController {
             state.enabledNetworkMap[namespace][storageKey] = false;
         });
     }
+    /**
+     * Restores the enabled network map to a previously snapshotted state.
+     *
+     * Not a general merge API: only updates keys already present in the current
+     * map. Missing snapshot values default to `false`. Intended for callers with
+     * direct controller access (e.g. extension) to undo `#onAddNetwork` filter
+     * switches when adding a network without changing the active selection.
+     *
+     * @param enabledNetworkMap - Previously snapshotted enabledNetworkMap.
+     */
+    restoreEnabledNetworkMap(enabledNetworkMap) {
+        this.update((state) => {
+            Object.entries(state.enabledNetworkMap).forEach(([namespace, currentNetworks]) => {
+                Object.keys(currentNetworks).forEach((chainId) => {
+                    const storageKey = chainId;
+                    const previousValue = enabledNetworkMap[namespace]?.[storageKey];
+                    state.enabledNetworkMap[namespace][storageKey] =
+                        previousValue ?? false;
+                });
+            });
+        });
+    }
     /**
      * Checks if a network is enabled.
      *
diff --git a/dist/NetworkEnablementController.d.cts b/dist/NetworkEnablementController.d.cts
index d45edfa8b59b78b216365cb1f8accae09faf3d6d..897a6272c61bccda2d2c0db24e6342903652d762 100644
--- a/dist/NetworkEnablementController.d.cts
+++ b/dist/NetworkEnablementController.d.cts
@@ -182,6 +182,17 @@ export declare class NetworkEnablementController extends BaseController<typeof c
      * - A CAIP-2 chain ID (e.g., 'eip155:1' for Ethereum mainnet, 'solana:mainnet' for Solana)
      */
     disableNetwork(chainId: Hex | CaipChainId): void;
+    /**
+     * Restores the enabled network map to a previously snapshotted state.
+     *
+     * Not a general merge API: only updates keys already present in the current
+     * map. Missing snapshot values default to `false`. Intended for callers with
+     * direct controller access (e.g. extension) to undo `#onAddNetwork` filter
+     * switches when adding a network without changing the active selection.
+     *
+     * @param enabledNetworkMap - Previously snapshotted enabledNetworkMap.
+     */
+    restoreEnabledNetworkMap(enabledNetworkMap: NetworkEnablementControllerState['enabledNetworkMap']): void;
     /**
      * Checks if a network is enabled.
      *
diff --git a/dist/NetworkEnablementController.d.mts b/dist/NetworkEnablementController.d.mts
index ae00ba562bcd69d36d0401b734d71f18ea4556ec..1155bf6d28c8fa881ad83e662a44c3401744babc 100644
--- a/dist/NetworkEnablementController.d.mts
+++ b/dist/NetworkEnablementController.d.mts
@@ -182,6 +182,17 @@ export declare class NetworkEnablementController extends BaseController<typeof c
      * - A CAIP-2 chain ID (e.g., 'eip155:1' for Ethereum mainnet, 'solana:mainnet' for Solana)
      */
     disableNetwork(chainId: Hex | CaipChainId): void;
+    /**
+     * Restores the enabled network map to a previously snapshotted state.
+     *
+     * Not a general merge API: only updates keys already present in the current
+     * map. Missing snapshot values default to `false`. Intended for callers with
+     * direct controller access (e.g. extension) to undo `#onAddNetwork` filter
+     * switches when adding a network without changing the active selection.
+     *
+     * @param enabledNetworkMap - Previously snapshotted enabledNetworkMap.
+     */
+    restoreEnabledNetworkMap(enabledNetworkMap: NetworkEnablementControllerState['enabledNetworkMap']): void;
     /**
      * Checks if a network is enabled.
      *
diff --git a/dist/NetworkEnablementController.mjs b/dist/NetworkEnablementController.mjs
index 4a29e5ba966d608445cd223b667ad403aa48babb..f3530073442bd10897872e84a301b9ecd112c7aa 100644
--- a/dist/NetworkEnablementController.mjs
+++ b/dist/NetworkEnablementController.mjs
@@ -405,6 +405,28 @@ export class NetworkEnablementController extends BaseController {
             state.enabledNetworkMap[namespace][storageKey] = false;
         });
     }
+    /**
+     * Restores the enabled network map to a previously snapshotted state.
+     *
+     * Not a general merge API: only updates keys already present in the current
+     * map. Missing snapshot values default to `false`. Intended for callers with
+     * direct controller access (e.g. extension) to undo `#onAddNetwork` filter
+     * switches when adding a network without changing the active selection.
+     *
+     * @param enabledNetworkMap - Previously snapshotted enabledNetworkMap.
+     */
+    restoreEnabledNetworkMap(enabledNetworkMap) {
+        this.update((state) => {
+            Object.entries(state.enabledNetworkMap).forEach(([namespace, currentNetworks]) => {
+                Object.keys(currentNetworks).forEach((chainId) => {
+                    const storageKey = chainId;
+                    const previousValue = enabledNetworkMap[namespace]?.[storageKey];
+                    state.enabledNetworkMap[namespace][storageKey] =
+                        previousValue ?? false;
+                });
+            });
+        });
+    }
     /**
      * Checks if a network is enabled.
      *
diff --git a/dist/constants.cjs b/dist/constants.cjs
index 0ce368f00d98bcb38f43e9c7fb608b51b955e7c5..134eaa14da1f5f02e70421b6a866a2c3440db834 100644
--- a/dist/constants.cjs
+++ b/dist/constants.cjs
@@ -17,5 +17,6 @@ exports.POPULAR_NETWORKS = [
     '0x8f', // Monad (143)
     '0x10e6', // MegaETH (4326)
     '0x13b2', // Arc (5042)
+    '0x1237', // Robinhood (4663)
 ];
 //# sourceMappingURL=constants.cjs.map
\ No newline at end of file
diff --git a/dist/constants.mjs b/dist/constants.mjs
index 476d382fbfa0cc052a66aae03a58473af8c9f118..96b817adb8bccdfbba3f9f3fca7aa67325ef4df2 100644
--- a/dist/constants.mjs
+++ b/dist/constants.mjs
@@ -14,5 +14,6 @@ export const POPULAR_NETWORKS = [
     '0x8f', // Monad (143)
     '0x10e6', // MegaETH (4326)
     '0x13b2', // Arc (5042)
+    '0x1237', // Robinhood (4663)
 ];
 //# sourceMappingURL=constants.mjs.map
\ No newline at end of file
