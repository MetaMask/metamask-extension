name: CI
on: [push]
jobs:
  shellcheck:
    name: Shellcheck
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: run
        run: |
          ./development/shellcheck.sh
