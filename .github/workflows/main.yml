name: Main

on:
  push:
    branches:
      - main
      - master
      - Version-v*
      - trigger-ci*
      - prep-deps*
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  merge_group:

jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - name: Setup environment
        uses: metamask/github-tools/.github/actions/checkout-and-setup@caching2
        with:
          is-high-risk-environment: 'false'
          ref: 'renaming-node'

      - run: cat package.json
        shell: bash

  test2:
    runs-on: ubuntu-latest
    steps:
      - name: Setup environment
        uses: metamask/github-tools/.github/actions/checkout-and-setup@caching2
        with:
          is-high-risk-environment: 'false'

      - run: cat package.json
        shell: bash

  test3:
    runs-on: ubuntu-latest
    steps:
      - name: Setup environment
        uses: metamask/github-tools/.github/actions/checkout-and-setup@caching2
        with:
          is-high-risk-environment: 'false'
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - run: cat package.json
        shell: bash

  test4:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - run: echo ${{ hashFiles('README.md') == '' }}
        shell: bash

      - run: echo ${{ hashFiles('.git') == '' }}
        shell: bash

      - run: echo ${{ hashFiles('.git') }}
        shell: bash
