name: Repository health checks

on:
  workflow_call:

jobs:
  repository-health-checks:
    name: Repository health checks
    runs-on: ubuntu-latest
    env:
      # For a `pull_request` event, the branch is `github.head_ref``.
      # For a `push` event, the branch is `github.ref_name`.
      BRANCH: ${{ github.head_ref || github.ref_name }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # TEST OVERRIDE: disable issue creation. Remove before merging to main.
      CREATE_TRACKING_ISSUE: 'false'
    steps:
      - name: Checkout and setup environment
        uses: MetaMask/action-checkout-and-setup@v3
        with:
          is-high-risk-environment: false
          # fetch-depth: 0 # This is needed to checkout all branches
          skip-allow-scripts: true

      # TEST OVERRIDES: The steps below are temporarily disabled for testing.
      # Restore all commented-out steps before merging to main.

      # - name: ShellCheck Lint
      #   if: ${{ !cancelled() }}
      #   run: ./development/shellcheck.sh

      # - name: Validate changelog
      #   if: ${{ !startsWith(env.BRANCH, 'release/') }}
      #   run: yarn lint:changelog

      # - name: Validate release candidate changelog
      #   if: ${{ startsWith(env.BRANCH, 'release/') }}
      #   run: .github/scripts/validate-changelog-in-rc.sh

      # - name: Lint lockfile
      #   if: ${{ !cancelled() }}
      #   run: yarn lint:lockfile

      # - name: Lint console baseline
      #   if: ${{ !cancelled() }}
      #   run: yarn lint:baseline

      # - name: Check yarn resolutions
      #   if: ${{ !cancelled() }}
      #   run: yarn --check-resolutions

      # --- Baseline download (PR only) ---
      # When the download fails (first rollout, expired artifact, or fork PR
      # with read-only token), NO_BASELINE=true tells the triage script to
      # fall back to the same criteria as the package.json `audit` script:
      #   yarn audit --environment production --severity moderate
      #
      # merge_group runs skip the diff entirely: by the time a PR enters the
      # merge queue it has already passed the diff check. Any new advisory
      # that appeared on main since then wasn't introduced by this PR and
      # will be surfaced by the next push-to-main run.
      - name: Find latest push-to-main run with baseline artifact
        id: find-baseline-run
        if: ${{ github.event_name == 'pull_request' }}
        continue-on-error: true
        uses: actions/github-script@v8
        with:
          script: |
            // Security: we must use an explicit push-to-main run ID when
            // downloading the baseline artifact. If we omit run-id,
            // actions/download-artifact defaults to the current run only.
            // If we searched all runs without the branch+event filter, a PR
            // branch run could upload a poisoned audit-baseline artifact that
            // whitelists arbitrary GHSA IDs, causing the diff to report zero
            // new advisories.
            const { data } = await github.rest.actions.listWorkflowRuns({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'main.yml',
              branch: 'main',
              event: 'push',
              status: 'completed',
              per_page: 1,
            });
            const run = data.workflow_runs[0];
            if (!run) {
              core.setFailed('No completed push-to-main run found for main.yml');
              return;
            }
            core.info(`Using baseline from run ${run.id} (${run.html_url})`);
            core.setOutput('run-id', String(run.id));

      - name: Download audit baseline
        id: download-baseline
        if: ${{ github.event_name == 'pull_request' && steps.find-baseline-run.outcome == 'success' }}
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          name: audit-baseline
          path: /tmp
          run-id: ${{ steps.find-baseline-run.outputs.run-id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      # --- Audit ---
      - name: Run audit and triage
        if: ${{ !cancelled() }}
        env:
          NO_BASELINE: ${{ github.event_name == 'pull_request' && steps.download-baseline.outcome != 'success' }}
        run: yarn tsx .github/scripts/yarn-audit-and-triage.mts --output-file /tmp/audit-current.json

      # On push-to-main, rename the audit output to audit-baseline.json so the
      # uploaded artifact name matches what PR runs download to /tmp.
      - name: Rename audit baseline
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        run: mv /tmp/audit-current.json /tmp/audit-baseline.json

      - name: Upload audit baseline artifact
        if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
        uses: actions/upload-artifact@v7
        with:
          name: audit-baseline
          path: /tmp/audit-baseline.json

      # --- Diff (PR only, runs only when a real baseline was downloaded) ---
      - name: Run audit diff
        if: ${{ github.event_name == 'pull_request' && steps.download-baseline.outcome == 'success' }}
        run: |
          yarn tsx .github/scripts/yarn-audit-diff.mts \
            --baseline-file /tmp/audit-baseline.json \
            --current-file /tmp/audit-current.json

      # - name: Detect yarn lock deduplications
      #   if: ${{ !cancelled() }}
      #   run: yarn dedupe --check

      # - name: Run depcheck
      #   if: ${{ !cancelled() }}
      #   run: yarn depcheck

      # - name: Validate allow-scripts config
      #   if: ${{ !cancelled() }}
      #   run: yarn allow-scripts auto

      # - name: Check feature flag registry
      #   if: ${{ !cancelled() }}
      #   env:
      #     GITHUB_BASE_REF: ${{ github.base_ref }}
      #   run: yarn tsx .github/scripts/check-feature-flag-registry.ts

      - name: Check working tree
        if: ${{ !cancelled() }}
        run: |
          if ! git diff --exit-code; then
              echo "::error::Working tree dirty."
              exit 1
          fi

      # - name: Run fitness functions
      #   if: ${{ !cancelled() && github.event_name == 'pull_request'}}
      #   env:
      #     BASE_REF: ${{ github.event.pull_request.base.ref }}
      #   run: |
      #     # The following command generates a diff of changes between the common
      #     # ancestor of $BASE_REF and HEAD, and the current commit (HEAD), for
      #     # files in the current directory and its subdirectories. The output is
      #     # then saved to a file called "diff".
      #     git diff "$(git merge-base "origin/$BASE_REF" HEAD)" HEAD -- . > ./diff

      #     yarn fitness-functions ci ./diff
