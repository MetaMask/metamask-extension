name: Add team label

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  add-team-label:
    uses: metamask/github-tools/.github/workflows/add-team-label.yml@f51f860ab0f4fad4d0d06e17298e5d327f6d0ef1
    secrets:
      TEAM_LABEL_TOKEN: ${{ secrets.TEAM_LABEL_TOKEN }}
