name: Get release timelines

on:
  workflow_dispatch:
    inputs:
      version:
        required: true
        type: string
        description: The version of the release

jobs:
  get-release-timelines:
    name: Get release timelines
    runs-on: ubuntu-latest
    steps:
      - uses: MetaMask/github-tools/.github/actions/get-release-timelines@7e0d74bc7b79ce9601e52f58d5e4d36c19591df9 # v1.19.0
        with:
          version: ${{ inputs.version }}
          runway-app-id: ${{ secrets.RUNWAY_APP_ID }}
          runway-api-key: ${{ secrets.RUNWAY_API_KEY }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
