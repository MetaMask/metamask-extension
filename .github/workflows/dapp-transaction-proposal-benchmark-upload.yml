name: Dapp transaction proposal upload

on:
  workflow_call:
    secrets:
      EXTENSION_BENCHMARK_STATS_TOKEN:
        required: true

jobs:
  dapp-transaction-proposal-upload:
    name: Upload Dapp Transaction Proposal Benchmark Data
    runs-on: ubuntu-latest
    steps:
      - name: Checkout and setup environment
        uses: MetaMask/action-checkout-and-setup@v1
        with:
          is-high-risk-environment: true
          skip-allow-scripts: true
          yarn-custom-url: ${{ vars.YARN_URL }}

      - name: Download page load benchmark results
        uses: actions/download-artifact@v4
        with:
          name: wallet-pop-open-time-benchmark-results

      - name: Create expected page load benchmark results directory structure
        shell: bash
        run: |
          mkdir -p test-artifacts/benchmarks
          mv wallet-pop-open-time-benchmark-results.json test-artifacts/benchmarks/wallet-pop-open-time-benchmark-results.json

      - name: Upload benchmark results (historical data)
        run: .github/scripts/wallet-pop-open-time-benchmark-stats-commit.sh
        env:
          EXTENSION_BENCHMARK_STATS_TOKEN: ${{ secrets.EXTENSION_BENCHMARK_STATS_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
