name: Automated RCA

on:
  issues:
    types: [closed]

permissions:
  issues: write
  contents: read

jobs:
  automated-rca:
    name: Automated RCA
    runs-on: ubuntu-latest
    steps:
      - name: Automated RCA
        uses: MetaMask/github-tools/.github/actions/post-gh-rca@7e0d74bc7b79ce9601e52f58d5e4d36c19591df9 # v1.19.0
        with:
          repo-owner: ${{ github.repository_owner }}
          repo-name: ${{ github.event.repository.name }}
          issue-number: ${{ github.event.issue.number }}
          issue-labels: '["Sev0-urgent", "Sev1-high"]'
          github-token: ${{ secrets.GITHUB_TOKEN }}
